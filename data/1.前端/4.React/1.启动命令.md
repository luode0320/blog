```
To address all issues (including breaking changes), run:
  npm audit fix --force

Run `npm audit` for details.

Created git commit.

Success! Created binance-web at E:\binance-web
Inside that directory, you can run several commands:

  npm start
    Starts the development server.

  npm run build
    Bundles the app into static files for production.

  npm test
    Starts the test runner.

  npm run eject
    Removes this tool and copies build dependencies, configuration files
    and scripts into the app directory. If you do this, you can’t go back!

We suggest that you begin by typing:

  cd E:\binance-web
  npm start

Happy hacking!
Done
```

翻译:

```
为解决所有问题（包括破坏性更改），运行：
npm audit fix --force

运行 npm audit 获取详细信息。

已创建 Git 提交。

成功！已在 E:\binance-web 创建 binance-web。
在该目录中，您可以运行以下几个命令：

npm start
启动开发服务器。

npm run build
将应用程序打包成用于生产的静态文件。

npm test
启动测试运行程序。

npm run eject
删除此工具并将构建依赖项、配置文件和脚本复制到应用程序目录中。如果执行此操作，您将无法回退！

我们建议您从以下步骤开始：

cd E:\binance-web
npm start

祝您编程愉快！完成
```

